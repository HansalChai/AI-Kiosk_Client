import Link from "next/link";
import { NextPage } from "next";

const NotFound : NextPage = () => {
    return(
        <div>
            <div> 이 페이지는 존재하지 않는 페이지입니다. 다른페이지를 검색해 보세요.</div>
        </div>
    )
}

export default NotFound;